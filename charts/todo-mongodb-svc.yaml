apiVersion: v1
kind: Service
metadata:
  name: todo-mongodb
  namespace: todo-app
spec:
  type: ClusterIP
  ports:
    - port: 27017
      targetPort: 27017
  selector:
    app.kubernetes.io/name: mongodb
